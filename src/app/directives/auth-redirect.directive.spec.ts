import { AuthRedirectDirective } from './auth-redirect.directive';

describe('AuthRedirectDirective', () => {
  it('should create an instance', () => {
    const directive = new AuthRedirectDirective();
    expect(directive).toBeTruthy();
  });
});
